# build output
dist/
.astro/

# dependencies
node_modules/

# environment
.env
.env.production

# editors / OS
.DS_Store

# wrangler
.wrangler/
# isolated local D1 persistence used only by `npm run db:test` (see
# tests/db/README.md) — never the developer's seeded local DB.
.wrangler-test/
